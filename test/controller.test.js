import View from '../src/mvc/view/view.js';
import Model from '../src/mvc/model/model.js';
import Controller from '../src/mvc/controller/controller.js';


const options = {
    min: 0,
    max: 30000,
    disableValues: false,
    vertical: false,
    oneRunner: false,
    step: 50,
    value1: 5000,
    value2: 15000,
    widthScale: 600,
}


//let controller = new Controller(view, model)


/*describe('Тестирование Controller', function () {

    it('Создается экземпляр класса Controller', () => {
        expect(controller).toBeDefined();
    });

})*/